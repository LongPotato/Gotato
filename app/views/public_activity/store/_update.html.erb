<span class="glyphicon glyphicon-edit text-muted">
  <%= display_name(a.owner.name) %> <%= a.created_at.strftime('%H:%M:%S %m/%d/%Y') %>
</span>

<p class="activity-text">
  <% if a.trackable %>
    Updated store info for <%= link_to a.trackable.name.try(:titleize), user_store_path(current_user, a.trackable.id) %>.
  <% else %>
    Updated store's info that is currently deleted.
  <% end %>
</p>

<legend></legend>