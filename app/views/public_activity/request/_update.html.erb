<span class="glyphicon glyphicon-check text-muted">
  <%= display_name(a.owner.name) %> <%= a.created_at.strftime('%H:%M:%S %m/%d/%Y') %>
</span>

<p class="activity-text">
  <% if a.trackable %>
    Checked an order request <%= link_to ">>", a.trackable.url, target: "_blank" %>
  <% else %>
    Created a request that is currently deleted.
  <% end %>
</p>

<legend></legend>